# ROADMAP — starmap как сервис генерации карт неба

## Цель

Сейчас `main.py` — это **одноразовый скрипт**: при запуске грузит все данные, рисует
одну фиксированную карту всего неба (RA/DEC, 6000px) и сохраняет `map_big.png`.

Нужно превратить репозиторий в **долгоживущий сервис**, который:
- запускается один раз и держится в памяти (данные загружены заранее);
- слушает запросы от Telegram-бота через **MQTT**;
- генерирует карту под параметры запроса (местоположение / время / тип карты);
- отдаёт готовое изображение обратно боту;
- публикует свой статус доступности, чтобы бот знал, запущен сервис или нет.

Транспорт — **MQTT** (брокер уже работает на Raspberry Pi, бот к нему подключён,
паттерн «запрос/ответ + картинка в base64» в боте уже реализован для `/photo`).

Развёртывание на Pi — **без Docker** (Docker только для локальной разработки).

---

## Контракт с ботом (зафиксировать первым делом)

Это договор между двумя репозиториями — он не должен меняться в одностороннем порядке.

### Топики MQTT

| Топик | Направление | Назначение |
|-------|-------------|------------|
| `starmap/command` | бот → сервис | запрос на генерацию карты |
| `starmap/result`  | сервис → бот | готовая карта или ошибка |
| `starmap/status`  | сервис → бот | `online` / `offline` (**retained + LWT**) |

### Запрос (`starmap/command`)

```json
{
  "request_id": "1718635200",
  "lat": 55.75,
  "lon": 37.62,
  "datetime": "2026-06-17T22:00:00",
  "map_type": "horizon"
}
```

- `request_id` — обязателен, по нему бот сматчит ответ (механика `register_request` уже есть в боте).
- `lat` / `lon` — координаты наблюдателя.
- `datetime` — опционально, ISO8601; по умолчанию «сейчас».
- `map_type` — тип карты (см. раздел «Типы карт»).

### Ответ (`starmap/result`)

```json
{
  "request_id": "1718635200",
  "status": "ok",
  "image_base64": "..."
}
```

либо при ошибке:

```json
{
  "request_id": "1718635200",
  "status": "error",
  "error": "Краткое описание проблемы"
}
```

> Альтернатива base64 (оба сервиса на одном Pi): сохранять PNG в общий каталог и
> слать в ответе только путь (`"image_path": "/var/lib/starmap/out/<request_id>.png"`),
> бот читает файл с диска. Меньше нагрузка на брокер и память. Решение можно отложить —
> начать с base64 (меньше движущихся частей), перейти на путь при упоре в размер.

### Статус (`starmap/status`)

- При старте сервис публикует **retained** `{"status":"online"}`.
- В **LWT** регистрирует `{"status":"offline"}` — брокер сам разошлёт его, если сервис упадёт.
- При штатном завершении — публикует `offline` перед disconnect.

---

## Задачи

### 1. Конфигурация
- [ ] Добавить `config.py` (или использовать `.env` + `python-dotenv`): `MQTT_BROKER`,
      `MQTT_PORT`, топики, путь к каталогу вывода, дефолтное разрешение, уровень логирования.
- [ ] Завести `.env.example` (сейчас `.env` пустой).
- [ ] Настроить логирование (`logging`), без `print`.

### 2. Загрузка данных один раз на старте
- [ ] Вынести инициализацию стиля и загрузку тяжёлых данных
      (`stars...parquet` — 53 МБ, `de421.bsp` — 16 МБ, ongc — 15 МБ и т.д.) в модуль,
      который исполняется **один раз** до входа в MQTT-цикл.
- [ ] Убедиться, что starplot не перечитывает parquet/bsp на каждый рендер
      (профилировать первый и повторный вызов на Pi).
- [ ] Зафиксировать `MPLBACKEND=Agg` (без дисплея).

### 3. Рендер с параметрами (главная содержательная работа)
- [ ] Вынести логику из `main.py` в функцию `render_map(lat, lon, dt, map_type) -> bytes`.
- [ ] Экспорт в память: `p.export(BytesIO(), format="png")` вместо записи файла
      (или в общий каталог, см. контракт).
- [ ] Понизить `resolution` для Telegram (2000–3000px вместо 6000) — быстрее и легче по весу.
- [ ] Реализовать привязку к местоположению и времени наблюдателя
      (текущий `main.py` рисует фиксированную RA/DEC карту всего неба, без наблюдателя).

#### Типы карт (`map_type`)
- [x] `full` — полносферная RA/DEC карта (`MapPlot`, Miller).
- [x] `galactic` — всё небо в галактических координатах (`GalaxyPlot`, Mollweide).
- [x] `zenith` — купол неба над наблюдателем (`ZenithPlot` + `.horizon()` + `.info()`).
- [x] `horizon` — вид над горизонтом из точки наблюдателя (`HorizonPlot`); направление через `options.direction`.
- [x] `optic` — объект в конкретную оптику (`OpticPlot`); бинокль/телескоп/камера.
- [ ] `optic`: резолв имени объекта (`M31`, `Юпитер`) в ra/dec — сейчас требуется явные `target.ra`/`target.dec`.
- [ ] Локализация подписей на русский (`settings.language = "ru"` — включить и проверить поддержку в starplot).
- [x] **Прогон рендеров вживую в Docker** (2026-06-17): все 5 типов рендерятся корректно (starplot 0.20.4).
      Найдено и исправлено: `settings.data_path` должен быть `Path` (не `str`); `ZenithPlot.info()`
      сломан в 0.20.4 (обращается к `self.dt`) — вызов убран; `wait_for_publish` на shutdown мог
      падать при разрыве — обёрнут в try/except.
- [ ] **base64 inline не лезет в MQTT** для больших карт (oversize packet → залипание на QoS1 retry).
      Перешли на `output.mode = file` по умолчанию. Если нужен base64 — добавить size-guard, чтобы
      oversize отдавал ошибку, а не вешал соединение.

### 4. MQTT-сервис
- [ ] `service.py`: единый клиент paho-mqtt с `client_id="starmap-service"`.
- [ ] `on_connect` → подписка на `starmap/command` (qos=1) + публикация retained `online`.
- [ ] Настроить **LWT** (`will_set` на `starmap/status` = `offline`, retained).
- [ ] `on_message` → распарсить JSON, провалидировать обязательные поля, поставить в обработку.
- [ ] Публикация ответа в `starmap/result` с тем же `request_id`.
- [ ] Reconnect с backoff при разрыве (можно подсмотреть реализацию в боте `mqtt_service.py`).
- [ ] `loop_forever` в основном потоке (сервис самостоятельный, блокироваться можно).

### 5. Конкурентность и устойчивость
- [ ] **Один рендер за раз**: `threading.Lock` или однопоточная очередь — matplotlib
      не потокобезопасен, и Pi не потянет параллельные рендеры.
- [ ] Если приходит запрос во время генерации — либо ставить в очередь, либо отвечать
      `error: "busy"` (выбрать стратегию; для начала — короткая очередь / busy).
- [ ] Обернуть генерацию в try/except, при сбое слать `status: error`, не падать.
- [ ] Тайм-бюджет: помнить, что бот ждёт ответ ~90–120с — рендер должен укладываться.

### 6. Управление выводом
- [ ] Если идём через файлы — создать каталог вывода, чистить старые PNG
      (по возрасту/количеству), чтобы не забивать SD-карту Pi.
- [ ] `.gitignore`: добавить каталог вывода и `map_big.png` (артефакты не коммитим).

### 7. Зависимости
- [ ] Добавить `paho-mqtt` и (при необходимости) `python-dotenv` в `requirements.txt`.
- [ ] Зафиксировать версии (сейчас в `requirements.txt` версии не закреплены).

### 8. Развёртывание на Raspberry Pi (без Docker)
- [ ] Инструкция по запуску: venv, `pip install -r requirements.txt`, `python service.py`.
- [ ] **systemd unit** для автозапуска и рестарта (`Restart=always`), переменные окружения,
      `MPLBACKEND=Agg`. Это и обеспечит, что LWT-статус корректно отражает «сервис жив».
- [ ] Проверить потребление RAM на Pi после загрузки данных (parquet + bsp в памяти).
- [ ] Обновить `Dockerfile`/`docker-compose.yml` под локальный запуск сервиса
      (сейчас `CMD ["python", "test_map.py"]` — файл отсутствует; поправить на `service.py`).

### 9. Тестирование
- [ ] Юнит-тест `render_map` (генерит непустой PNG при валидных координатах).
- [ ] Тест парсинга/валидации команды (битый JSON, отсутствующие поля → `error`, не падение).
- [ ] Ручной тест end-to-end: опубликовать тестовую команду в `starmap/command`
      (`mosquitto_pub`) и проверить ответ в `starmap/result` (`mosquitto_sub`).
- [ ] Проверить поведение LWT: убить процесс и убедиться, что в `starmap/status` пришёл `offline`.

### 10. Документация
- [ ] `README.md` с описанием сервиса, контракта топиков и запуска (сейчас README пуст).
- [ ] Зафиксировать формат запроса/ответа рядом с кодом, чтобы бот и сервис не разъезжались.

---

## Порядок выполнения (предлагаемый)

1. Контракт топиков и JSON (раздел выше) — согласовать с ботом.
2. Рефактор `main.py` → `render_map(...) -> bytes` с предзагрузкой данных (разделы 2–3).
3. MQTT-обёртка `service.py` + статус/LWT (раздел 4).
4. Lock на один рендер + обработка ошибок (раздел 5).
5. Зависимости, .env, systemd, деплой на Pi (разделы 1, 7, 8).
6. Тесты + README (разделы 9, 10).

> Параметризация под местоположение/время (раздел 3) — самый объёмный пункт:
> текущий скрипт рисует фиксированную карту без наблюдателя, и именно эту логику
> нужно переписать на основе starplot `HorizonPlot`/зенитных карт.
